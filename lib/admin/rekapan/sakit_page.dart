import 'package:flutter/material.dart';

class RekapanSakit extends StatelessWidget {
  const RekapanSakit({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Rekapan Sakit")),
      body: Center(child: Text("Halaman untuk rekapan sakit")),
    );
  }
}
