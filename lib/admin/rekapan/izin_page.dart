import 'package:flutter/material.dart';

class RekapanIzin extends StatelessWidget {
  const RekapanIzin({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Rekapan Izin")),
      body: Center(child: Text("Halaman untuk rekapan izin")),
    );
  }
}
