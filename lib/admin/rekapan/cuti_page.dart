import 'package:flutter/material.dart';

class RekapanCuti extends StatelessWidget {
  const RekapanCuti({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Rekapan Cuti")),
      body: Center(child: Text("Halaman untuk rekapan cuti")),
    );
  }
}
