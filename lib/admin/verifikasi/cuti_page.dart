import 'package:flutter/material.dart';

class VerifikasiCuti extends StatelessWidget {
  const VerifikasiCuti({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Verifikasi Cuti")),
      body: Center(child: Text("Halaman untuk verifikasi cuti")),
    );
  }
}
