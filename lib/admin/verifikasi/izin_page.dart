import 'package:flutter/material.dart';

class VerifikasiIzin extends StatelessWidget {
  const VerifikasiIzin({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Verifikasi Izin")),
      body: Center(child: Text("Halaman untuk verifikasi izin")),
    );
  }
}
