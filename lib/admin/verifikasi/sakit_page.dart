import 'package:flutter/material.dart';

class VerifikasiSakit extends StatelessWidget {
  const VerifikasiSakit({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Verifikasi Sakit")),
      body: Center(child: Text("Halaman untuk verifikasi sakit")),
    );
  }
}
